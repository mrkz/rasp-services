---
all:
  children:
    homelab:
      hosts:
        rpi4:
          # ansible_host and host_ip are both defined in host_vars/rpi4.yml.
          # The filename must match the hostname key above exactly.
          # If you rename this host, rename host_vars/rpi4.yml to match.
          ansible_user: root
